export namespace POLICY_MODAL_VIEWS {
    let SELECTION: string;
    let TEMPLATES: string;
    let EDITOR: string;
    let REVIEW: string;
}
/**
 * Vordefinierte RLS Policy Templates
 * @type {Array<PolicyTemplate>}
 */
export const POLICY_TEMPLATES: Array<PolicyTemplate>;
